# 玄象所 命理整合網站

一個整合星座、塔羅、八字、姓名學、紫微斗數的命理原型網站，前端輸入資料後會呼叫 AI 生成個人化解讀。

## 這個資料夾要部署到 GitHub + Vercel（推薦方式）

```
MILI-V25/
├── index.html          ← 前端網頁（星座/塔羅/八字/姓名學/紫微斗數）
├── vendor/
│   ├── iztro.min.js    ← 紫微斗數排盤引擎（開源套件，內建在專案裡，不依賴外部 CDN）
│   └── lunar.js        ← 八字排盤引擎（開源套件 lunar-javascript，內建在專案裡，不依賴外部 CDN）
├── api/
│   └── interpret.js    ← Vercel 無伺服器函式，負責安全呼叫 Google Gemini API（金鑰放這裡，不會外流）
├── package.json         ← 標記為 ES Module，Vercel 部署 api/interpret.js 需要用到
└── README.md            ← 就是你現在看的這份
```

**部署前記得自己建立這兩個檔案**（Vercel 環境變數才是金鑰真正該放的地方，不要把金鑰寫進任何檔案裡）：
- `.gitignore`：內容寫 `.env` 跟 `.env.local` 兩行就好，避免不小心把本機測試用的金鑰檔案傳上 GitHub
- `.env.local`（僅本機測試用，正式環境用 Vercel 的 Environment Variables）：內容為 `GEMINI_API_KEY=你的金鑰`

## AI 解讀用的是哪個服務？（V2 更新）
V2 開始改用 **Google Gemini API**（`gemini-2.5-flash` 模型），原因是它有免費額度、不用綁信用卡，適合這種小流量的原型網站。

- 免費額度大約每分鐘 10-15 次請求、每天數百到 1500 次不等（Google 會不定期調整，正式上線前建議到 Google AI Studio 確認當下實際額度）
- 免費額度的輸入內容 Google 可能會用於改善模型，如果之後有隱私顧慮，可以在 Google Cloud 專案開啟計費來關閉這項條款
- 如果流量常常撞到限額，把 `api/interpret.js` 裡的 `MODEL` 常數改成 `"gemini-2.5-flash-lite"` 即可拿到更高的免費額度（品質略陽春）
- 金鑰申請：https://aistudio.google.com/apikey

### 撞到「AI 解讀請求太多（已達免費額度上限）」怎麼辦？（V23 新增，不用付費的解法）
V23 從三個方向一起下手，讓免費額度撐更久，同時使用者體驗也更好：
1. **回應快取**：一模一樣的輸入（同星座＋同關注面向、同一組五格數理等）4 小時內重複問，會直接回傳上次的解讀，完全不消耗 AI 額度，速度也更快。
2. **更多備援模型**：`FALLBACK_MODELS` 清單新增了 `gemini-3-flash-preview`，跟原本的 `gemini-2.5-flash-lite`、`gemini-3.1-flash-lite`、`gemini-flash-latest` 一起，額度是各自獨立計算，等於把好幾個模型的免費額度加總起來用。
3. **Groq 免費備援（選用）**：Gemini 全部模型都是同一個 Google 專案共用總額度，流量大時可能整批一起用完；Groq 是完全不同公司、獨立計算免費額度，等於多一組備用油箱，一樣不用付費、不用綁卡：
   - 到 https://console.groq.com/keys 註冊帳號並建立一組免費 API Key
   - 到 Vercel 專案 → Settings → Environment Variables 新增 `GROQ_API_KEY`，貼上金鑰
   - 不設定這個變數完全不影響原本功能，只是少了這道最後備援
4. 前端也新增了自動重試：真的撞到 429 時，畫面會顯示倒數秒數，15 秒後自動重試一次、還不行再等 30 秒重試一次，不用使用者自己手動再按按鈕。

如果流量真的持續很大、上面幾招還是常常撞牆，代表免費額度本身已經不夠這個網站的實際使用量，這時候只剩「付費」或「減少功能／限制使用人數」兩個方向了，沒有其他不用付費又能無限使用的辦法。


## 版本紀錄
- **V35（目前版本）**：頁首 LOGO 換成使用者提供的新版盾牌＋電路紋路造型 ELVIS 圖標。原圖背景是接近純黑的深色漸層，直接貼上去會在頁首出現一塊突兀的方形色塊，所以先用去背處理：以每個像素 RGB 最大值當作亮度依據，把偏暗的背景區域轉成漸層透明（越暗越透明、保留亮部的霓虹光暈與文字），再裁掉四周多餘的透明邊界、縮放到頁首合適的顯示尺寸，這樣 LOGO 就能自然融入頁首本來的深紫色底（`rgba(20,18,43,0.85)`），不會有背景色塊的違和感。另外把整個品牌區（LOGO＋「艾維斯玄象所 / ELVIS ORACLE」文字）包成一個連結，點擊會在新分頁開啟 `https://elvis2026-maker.github.io/MIS/`（艾維斯資訊 IT 顧問官網，跟頁尾原本的連結一致），滑鼠移上去有淡出效果提示可以點擊。
- V34：①主標題「玄象所」改為「艾維斯玄象所」，並在 `<title>`、頁首品牌區與頁尾都同步更新；頁首品牌區改成中英文雙行顯示，logo 右側上排是中文「艾維斯玄象所」、下排新增英文名稱「ELVIS ORACLE」（沿用命理神諭 Oracle 的意象），寫法比照艾維斯其他站點（如「艾維斯上班所 / ELVIS WORKDESK」）的中英並列風格。②頁尾大改版精簡：原本燙金銘牌（掃光動畫方塊）＋額外一行 build-info 的兩層結構，合併成單一行「Elvis 程式設計 ‧ 艾維斯資訊顧問 IT ‧ 版次 ‧ 日期」，配色與字體維持原本的暗金色 `Space Mono` 風格不變，只是排版更簡潔；原本的超連結（點擊前往作者頁面）保留在「Elvis 程式設計」文字上。日期顯示格式也從 `2026.07.22` 改成 `2026-07-22` 以符合這行的呈現方式。
- V33：延續 V31 的方向，這次處理「AI 解讀失敗時」使用者實際會看到的文字。之前如果 Gemini 全部備援模型與 Groq 都失敗、或連不上後端，畫面上會直接顯示 `err.error` 原文，可能出現「AI 服務暫時無法回應」「無法連線到後端服務」這類技術字眼，對一個命理網站來說觀感生硬、也容易讓人誤以為網站壞了。現在改成：①前端 `askClaude()` 除了「輸入內容過長」這種使用者自己能修正的 400 錯誤仍照實顯示外，其餘所有失敗狀況（額度滿了、伺服器錯誤、連線失敗…）一律統一顯示同一句「天機此刻運行受阻，暫時無法為您解讀，請稍候片刻再試一次。」，不透露任何技術細節。②後端 `api/interpret.js` 裡原本會被當成解讀內容直接顯示的兩句話（Gemini 沒回應候選結果、安全過濾器擋下內容）也一併改成同調性的文字。技術性的 `error` 欄位本身不受影響，Vercel 的 Function Logs 還是能看到真正的錯誤原因，只是不會顯示在使用者畫面上。
- V32：星座運勢內容大幅加強，解決「內容太單薄、太制式」的問題。①新增 12 星座的延伸占星資料（元素／模式／守護星／幸運色與數字／最速配與需要磨合的星座），按下生成解讀的當下就先顯示，不用等 AI 回應，畫面立刻有紮實內容。②新增「想了解的時間範圍」切換（今日／本週／本月），會一併帶入 AI prompt，讓解讀貼合使用者實際想問的時間尺度。③ AI 回應改成結構化格式：愛情／事業／財運／健康四個分項，各自附上 1-5 分的量表卡（沿用八字「日主旺衰」量表的視覺樣式）與一句具體描述，不再只有一整段模糊的文字；另外抓出「本段時間關鍵字」與一句具體行動建議，做成醒目的重點提示卡（沿用塔羅牌的「重點提示」樣式）。④ 具備容錯設計：如果 AI 這次沒有照指定格式回覆（偶爾會發生），會自動降級成原本的整段文字顯示，畫面不會壞掉或空白。
- V31：清查全站文字，把使用者看得到的「AI 解讀」字樣改掉，避免出現生硬的技術用語。①紫微斗數結果區塊的預設提示文字「AI 解讀將顯示於此」改成「命盤解讀將顯示於此」，跟星座／塔羅／八字／姓名學其他分頁「結果將顯示於此」的用語風格一致。②頁尾「內容由 AI 生成」改成「內容由命理演算系統自動生成」。程式碼裡的註解（使用者看不到的部分）保留原樣未動。
- V30：修正「八字大運／流年時間軸」點擊卡片沒有反應的問題。根因是共用的放大檢視彈窗（`#zwModalOverlay`）當初被寫在紫微斗數那個分頁區塊（`.panel`）裡面，而沒有被選到的分頁本身是 `display:none`，導致只要使用者人在「八字」分頁點大運卡片，彈窗雖然有被正確產生內容，卻因為外層祖先是隱藏的而整個是 0×0、完全看不到、也點不到關閉鈕；只有剛好停留在「紫微斗數」分頁時才會正常顯示。修法是把這個共用彈窗搬到所有 `.panel` 之外（`main` 標籤外層），讓它在任何分頁底下都能正常顯示；已用無頭瀏覽器（Playwright）實際點過大運九張卡片與紫微十二宮各宮位確認皆恢復正常。
- V26：八字四柱大改版，換成開源 [lunar-javascript](https://github.com/6tail/lunar-javascript) 引擎（`vendor/lunar.js`）取代原本的簡化估算公式，四柱排盤會自動核對節氣，準確度大幅提升。①新增「四柱詳批表」：每柱的十神、藏干（含藏干十神）、五行、納音一次列出。②新增「日主旺衰＋用神喜忌」自動判斷：依十神生扶／洩耗的加權比例（月令得令額外加權）判斷身旺／身弱／中和，並給出用神、忌神的簡化建議方向（有清楚標示這是簡化版強弱判斷法）。③新增「神煞」：以日柱起算的天乙貴人、桃花、驛馬、華蓋、祿神、羊刃。④新增「四柱刑沖會合關係圖」：沿用姓名學五格圖的 SVG 連線動畫手法，畫出四柱地支之間的六合／六沖／相刑／三合／三會關係，並附圖例。⑤新增「大運／流年時間軸」：仿照紫微十二宮的滑動瀏覽手法，大運卡片可左右滑動、標示目前所在大運，點擊卡片會用同一套彈窗元件顯示該十年逐年流年干支。⑥五行分布圖沿用原本的五邊形動畫，但改吃 lunar-javascript 排出的精確四柱資料。⑦ AI 解讀 prompt 一併餵入十神、五行、納音、神煞、日主旺衰、目前大運，解讀內容更豐富、更貼近命盤細節。⑧ 修正 lunar-javascript 內建文字只有簡體中文的問題（十神、納音、生肖），新增簡轉繁對照表，畫面顯示與站內其餘文字一致皆為繁體。
- V25：頁尾銘牌「程式設計 Elvis Liu」文字加上超連結，點擊會在新分頁開啟 https://elvis2026-maker.github.io/MIS/ ，銘牌外觀（燙金色澤、掃光動畫）維持不變，只是文字變成可點擊的連結。
- V24：首頁副標語文案改寫——原本「整合星座、塔羅、八字、姓名學與紫微斗數，結合結構化命理數據與玄學邏輯推演，為你生成專屬命理解析。」讀起來像制式 AI 生成文案，改成直接點出各系統的專業術語：「融合黃道十二宮、塔羅大阿爾卡納、子平八字、姓名五格三才與紫微十四主星，依循命理古法排盤演算，推衍屬於你的命盤脈絡。」
- V23：解決「AI 解讀請求太多（已達免費額度上限）」的問題，且不需要付費——① 後端新增回應快取（4 小時內同樣的輸入直接沿用上次解讀，不再重打 API）。② `FALLBACK_MODELS` 新增免費模型 `gemini-3-flash-preview`，可用的免費額度更多。③ 新增選用的 Groq 免費備援（需自行申請免費金鑰、設定 `GROQ_API_KEY`），在 Gemini 全部模型都額滿時當最後一道防線，Groq 是完全獨立公司計算的免費額度，等於多一組備用油箱。④ 前端 `askClaude` 新增自動重試：撞到 429 時畫面會顯示倒數秒數，15 秒、30 秒後各自動重試一次，不用使用者自己手動再按。詳見上方「撞到『AI 解讀請求太多』怎麼辦？」段落。
- V17：加強後端 `/api/interpret` 的安全性，防止有人複製前端頁面後盜用你的 Gemini API 額度：① 新增來源網域白名單檢查，透過 `ALLOWED_ORIGIN` 環境變數指定唯一允許呼叫這支 API 的網域，其他網域發出的請求會被擋下（回傳 403）。② 新增簡易的同 IP 頻率限制（10 分鐘內最多 20 次請求），超過會回傳 429 訊息。③ 移除先前除錯用的金鑰內容診斷 log。④ 說明：Gemini API 金鑰原本就只存在 Vercel 伺服器端環境變數裡，從未出現在瀏覽器看得到的前端程式碼中，這點在 V3 修正金鑰傳遞方式時就已經是這樣設計，這次是在此基礎上再加上「誰能呼叫」與「呼叫幾次」的限制。⑤ 網頁前端本身（HTML/CSS/JS）在技術上無法做到完全禁止下載或查看原始碼，瀏覽器本來就得先收到這些程式碼才能顯示網頁；經與網站擁有者確認，不加裝右鍵/選取限制等嚇阻性措施，以維持正常使用體驗。

### 設定 ALLOWED_ORIGIN（V17 新增，強烈建議設定）
1. 到 Vercel 專案 → **Settings → Environment Variables**
2. 新增一筆：
   - Key：`ALLOWED_ORIGIN`
   - Value：你的正式網站網址，例如 `https://star-seven-tan.vercel.app`（有接自訂網域就填自訂網域；結尾不要加斜線）
   - Environments：勾選 Production（建議連 Preview、Development 一起勾）
3. 存檔後，回 **Deployments** 頁面對最新一筆按 **Redeploy** 讓設定生效
4. 沒有設定這個變數時，白名單檢查會自動略過（不會擋到你自己），但別人也就能直接呼叫你的 API——建議部署後盡快設定好。

- V13：解讀內文（`.reading`，所有分頁生成的 AI 解讀文字都是這個樣式）字重從細體（300）改成正常體（400）。原本文字顏色其實已經是最深的墨色、對比度數值上不低，但因為中文字筆畫較多，用細體字顯示大段落文字時容易看起來「發虛」、長時間閱讀比較費眼力，改成正常字重後筆畫更扎實、閱讀起來更省力，不用再調整顏色或加大字級。
- V12：解決紫微命盤在小螢幕上「字太小看不清楚」的問題——① 命盤格子的最小尺寸與所有文字都調大一級（主星名稱、宮位名稱從 13px 調到 14.5px，其餘輔星/雜曜/大限文字也都放大）。② 新增「點擊放大」功能：點/點選任一宮位，會跳出一個置中的大視窗，用更大的字級（主星 19px）完整重新排版顯示該宮位的主星、輔星煞星、雜曜、大限、小限，點視窗外側或按 Esc 都能關閉，手機上不用再瞇眼看小字。③ 命盤下方新增操作提示文字，告知使用者可以左右滑動＋點擊放大。
- V11：紫微斗數全面改版——① 排盤引擎改用開源的 [iztro](https://github.com/SylarLong/iztro)（MIT License，已將 `iztro.min.js` 內建在 `vendor/` 資料夾，不依賴外部 CDN），輸入改成跟八字一致的「國曆出生日期＋時／分＋性別」，取代原本只能填農曆月份與時辰的陽春版本。② 新增完整十二宮命盤圖表：十四主星＋輔星（左輔右弼、文昌文曲、天魁天鉞）＋煞星（擎羊陀羅、火星鈴星、地空地劫）＋雜曜全部排出，命宮／身宮會特別標示並加上邊框強調，四化（祿權科忌）以彩色圓形標記在對應星曜旁，每個宮位也會顯示大限歲數範圍與小限。③ AI 解讀改成讀取命宮／財帛／官祿／夫妻／疾厄／福德六個關鍵宮位的實際主星與四化資料，並會自動算出使用者目前的虛歲、對應到哪個大限一併納入提示詞，解讀內容比舊版（只給命宮地支）更準確、更個人化。④ 命盤圖表較寬，手機上會自動變成左右滑動瀏覽，不會被擠壓變形。
- V10：星座運勢生成解讀時，會在結果欄上方畫出對應星座的原創 SVG 占星符號（例如牡羊座的雙角、雙魚座的雙魚等），外圍搭配雙圈刻度環（各自反向緩慢旋轉）與四個角落的星芒閃爍動畫，符號本身則以縮放＋淡入方式揭曉，呈現更有星象儀式感；同時將星座／八字出生日期欄位的預設範例從舊版寫死的日期改成通用示範日期 `19990101`，避免誤植成特定真實生日。
- V9：星座與八字的「出生日期」欄位改成可直接輸入 8 碼數字（例如 `19990101`），輸入時會自動補上斜線分隔，取代原本必須點開日曆選擇的方式，操作更直覺；同時會檢查日期是否真實存在（例如不會誤收 2 月 30 日）。八字的「出生時間」改用「時／分」兩個下拉選單取代原生時間選擇器，跨瀏覽器、跨裝置的顯示與操作更一致。
- V8：① 八字「五行分布圖」從長條圖改成正五邊形雷達圖——木火土金水依「相生」順序排在五邊形的五個頂點，資料多邊形會用 `requestAnimationFrame` 從中心展開到實際比例，背景另外畫出對應「相剋」順序的虛線星形參考線，視覺上更貼近命理上「五行」的概念，也更精簡地把五個數值的比例關係一次呈現出來。② 塔羅新增兩項功能：(a) 22 張大阿爾卡納牌翻牌後會顯示一枚原創的簡化 SVG 線條插圖（例如太陽、月亮、命運之輪、高塔等），逆位時插圖會整個旋轉 180 度，正位／逆位的顏色也不同，讓抽到的牌更好辨識；(b) 三張牌翻開後，會先用一排「重點特寫卡」把過去／現在／未來三張牌的圖示＋牌名＋關鍵字整理出來，AI 完整解讀文字生成後，還會額外抓出最後一句（通常是具體建議）獨立做成醒目的「重點提示」區塊，跟下方完整內文分開，方便使用者快速抓住重點而不必先讀完整段文字。
- V7：① 八字「五行分布圖」改成真正的 JS 動畫（`requestAnimationFrame` 逐格計算），長條寬度與右側數字會一起從 0 跑到目標值，並在跑分過程中加上光澤掃過效果，不再依賴容易被瀏覽器合併重繪、導致動畫失效的 CSS transition 技巧。② 姓名學結果新增「大名字招牌」與五格圖：姓名會用大字放大顯示在結果欄最上方，下方則有一個十字佈局的五格圖（天格／人格／地格／外格／總格），五個數理會以連線＋逐一彈出的動畫依序呈現，視覺上更清楚也更有儀式感。③ 星座與紫微斗數在結果生成時，新增一個由上而下擴散的「炫光閃現」動畫，並將等待文字換成帶有旋轉符文圖示的讀取提示，讓結果揭曉更有戲劇效果；此光暈與跑出動畫（chip 逐一淡入）也套用到其餘分頁的結構化標籤上，整站風格更一致。
- V6：① 修正八字面板「五行分布圖」長條偶爾不會跑動畫的問題——原本用兩層 `requestAnimationFrame` 觸發 CSS transition，在部分手機瀏覽器上會被瀏覽器合併成同一次重繪而不播放動畫，改成先強制觸發一次 reflow（讀取 `offsetWidth`）再套用目標寬度，動畫更穩定可靠。② 修正姓名學「手動輸入筆畫沒有用」的重大 bug——原本按下「計算五格並生成解讀」時，程式碼會無條件重新產生筆畫輸入欄位，把使用者剛剛手動填寫的筆畫值直接洗掉再讀取（永遠讀到空值或字典預設值），現在改成只有姓名字元真的改變時才重建欄位，手動輸入的筆畫會被正確保留並拿去計算。③ 筆畫字典新增一批常見名字用字（珍、維、冠、儀、雯…等），減少「未收錄請手動輸入」的情況。
- V3：修正 AI 解讀後端呼叫 Gemini API 的金鑰傳遞方式——改用官方建議的 `x-goog-api-key` Header（原本是網址參數 `?key=`），解決新版 `AQ.` 開頭金鑰可能出現的 400「API key not valid」錯誤；新增 `package.json` 解決部署時的 ESM 轉換警告訊息。
- V2：① 配色調整——頭尾（header／hero／footer）維持深色星空風格，中間的輸入／解讀面板改為宣紙淺色系，長時間閱讀更不傷眼，同時保留玄機氛圍。② AI 解讀後端改用 Google Gemini API（原本是 Anthropic），改用免費額度、不用綁信用卡，細節見下方「AI 解讀用的是哪個服務」。
- V1：全站深色星空風格 + Anthropic API（初版）。

## 目前已完成的功能
- ⭐ 星座：輸入生日自動判斷星座 + AI 生成運勢
- 🔮 塔羅：抽「過去／現在／未來」三張牌 + AI 綜合解讀
- 🗓 八字：輸入生日時間，lunar-javascript 引擎精確排出四柱干支＋十神／五行／納音／神煞／日主旺衰／大運流年 + AI 解讀
- ✍️ 姓名學：輸入姓名，計算天格/人格/地格/外格/總格五格數理 + AI 解讀
- ✨ 紫微斗數：輸入國曆出生日期時間與性別，排出完整十二宮命盤（十四主星／輔星／煞星／四化／大限）+ AI 解讀
- 手機版排版、金屬質感銘牌、動態星空背景、日期版次自動產生

## 已知限制（之後可以再優化）
- 八字：四柱排盤本身（V26 起）已改用 lunar-javascript 引擎，會自動核對節氣，準確度等同正規萬年曆；但「日主旺衰」與「用神喜忌」是簡化版強弱判斷法（依十神生扶洩耗的加權比例估算），並未納入通根、透干、刑沖合會對強弱的精細調整，僅供初步參考，非完整格局論斷；神煞也只收錄天乙貴人、桃花、驛馬、華蓋、祿神、羊刃等常見幾種，並非全套神煞
- 姓名學：筆畫字典只收錄常見字，生僻字會跳出手動輸入欄位讓使用者自己修正
- 塔羅牌的 SVG 插圖為簡化原創線條圖示，並非完整的塔羅牌美術風格，之後可以再加強成更精緻的插畫
- 紫微斗數：星曜亮度、四化與大限採 iztro 預設流派設定（較接近三合派），飛星派等其他流派算法可能略有出入；命盤本身的排演算法已是完整正規排法，這點跟八字／姓名學的簡化演算法性質不同


---

## 部署到 GitHub + Vercel 的完整步驟

### 1. 建立 GitHub Repository
到 https://github.com/new 建立一個新的 repository（可以設為 Private，只有你自己看得到程式碼）。

### 2. 把這個資料夾推上 GitHub

**電腦操作（推薦，最快）：**
```bash
cd MILI-V25
git init
git add .
git commit -m "V25：頁尾銘牌文字加上超連結"
git branch -M main
git remote add origin https://github.com/你的帳號/你的repo名稱.git
git push -u origin main
```
> 不熟指令的話，下載「GitHub Desktop」（https://desktop.github.com/），用拖拉介面把整個資料夾發布上去，效果一樣。

**只有手機的話：**
1. 用手機瀏覽器（不是 GitHub App）打開你的 repo 網頁
2. `index.html`：點 **Add file → Upload files**，從手機裡選這個檔案上傳
3. `vendor/iztro.min.js`：用 **Add file → Create new file**，檔名欄位直接打 `vendor/iztro.min.js`（帶斜線，GitHub 會自動建立 `vendor` 資料夾），這支檔案內容很長，建議還是用電腦或 GitHub Desktop 上傳比較不容易複製貼上出錯
3-1. `vendor/lunar.js`：同上，檔名打 `vendor/lunar.js`，內容一樣很長，建議用電腦或 GitHub Desktop 上傳
4. `api/interpret.js`：同樣用 **Add file → Create new file**，檔名打 `api/interpret.js`
5. `package.json`：同上，用 Create new file 建立
6. `.gitignore`：用 Create new file 建立，內容貼上兩行 `.env` 和 `.env.local`

**重要：`.env`／`.env.local` 檔案要被 `.gitignore` 排除，金鑰不會被上傳，這是故意的、也是必須的——金鑰只能存在 Vercel 的環境變數設定裡，絕對不要把金鑰貼進程式碼或上傳到 GitHub。**

### 3. 連接 Vercel
1. 到 https://vercel.com 用 GitHub 帳號登入
2. 點「Add New Project」→ 選擇你剛剛推上去的 repository
3. Framework Preset 選 "Other"（純 HTML，不是 React/Next.js）
4. 展開 "Environment Variables"，新增一筆：
   - Key: `GEMINI_API_KEY`
   - Value: 你的金鑰（AIza開頭那一串，申請網址在最下面）
   - （選用）如果也想啟用 Groq 免費備援，同樣方式再新增一筆 `GROQ_API_KEY`，金鑰到 https://console.groq.com/keys 免費申請，不設定也完全不影響網站運作
5. 點 Deploy

幾十秒後 Vercel 會給你一個網址，例如 `https://mingli-website.vercel.app`，打開就能直接用，AI 解讀功能也會正常運作（因為 `/api/interpret` 跟前端在同一個網域下，不用另外設定網址）。

### 4. 之後要更新網站怎麼做？
以後只要修改檔案、`git push` 上去（或用手機重新上傳/編輯該檔案），Vercel 會自動偵測並重新部署，不用手動操作。

### 5. 想換成自己的網域？
Vercel 專案設定裡有「Domains」，把你的網域（例如 mingli.elvis-mis.com）填進去，照指示到你網域註冊商那邊加一筆 DNS 紀錄即可，Vercel 會自動處理 HTTPS 憑證。

---

## 疑難排解

**部署後點按鈕沒反應/一直顯示「生成中」？**
- 打開瀏覽器開發者工具（電腦版）看 Console 有沒有錯誤訊息
- 確認 Vercel 專案的 Environment Variables 裡有正確設定 `GEMINI_API_KEY`
- 確認金鑰本身有效、免費額度沒有當下被撞滿（到 https://aistudio.google.com 檢查，或看 Vercel 的 Function Logs 有沒有錯誤訊息）

**手機瀏覽器打開網址列顯示 content://download 而不是網址？**
- 這代表你打開的是「下載到手機的本機檔案」，不是真正上線的網站。等部署到 Vercel 後，要用 Vercel 給你的網址（https://xxx.vercel.app）打開才是正式運作的版本，本機檔案沒辦法呼叫後端 API。

## 金鑰申請
https://aistudio.google.com/apikey

